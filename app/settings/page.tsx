'use client';
import {useEffect,useState} from 'react';import {toast} from 'sonner';import {Bell,User,Save} from 'lucide-react';import {useTheme} from 'next-themes';import {Sidebar} from '@/components/sidebar';import {Topbar} from '@/components/topbar';import {Button} from '@/components/ui/button';import {Input} from '@/components/ui/input';import {Label} from '@/components/ui/label';import {Select,SelectContent,SelectItem,SelectTrigger,SelectValue} from '@/components/ui/select';import {Card,CardContent,CardHeader,CardTitle} from '@/components/ui/card';
export default function SettingsPage(){const {theme,setTheme}=useTheme();const [name,setName]=useState('CRM Admin');const [email,setEmail]=useState('admin@example.com');const [notifications,setNotifications]=useState('All activity');useEffect(()=>{setName(localStorage.getItem('crm-profile-name')||'CRM Admin');setEmail(localStorage.getItem('crm-profile-email')||'admin@example.com');setNotifications(localStorage.getItem('crm-notifications')||'All activity')},[]);function save(){localStorage.setItem('crm-profile-name',name);localStorage.setItem('crm-profile-email',email);localStorage.setItem('crm-notifications',notifications);toast.success('Settings saved')}return <div className="flex min-h-screen"><Sidebar/><div className="flex min-w-0 flex-1 flex-col"><Topbar search="" onSearch={()=>{}} onFilters={()=>{}} count={0}/><main className="flex-1 space-y-6 p-4 md:p-6"><div><p className="text-sm font-medium text-primary">Workspace preferences</p><h1 className="mt-1 text-2xl font-semibold">Settings</h1><p className="mt-1 text-sm text-muted-foreground">Manage your profile and application preferences.</p></div><div className="grid max-w-4xl gap-4 lg:grid-cols-2"><Card><CardHeader><CardTitle className="flex items-center gap-2 text-base"><User className="h-4 w-4 text-primary"/>Profile settings</CardTitle></CardHeader><CardContent className="space-y-4"><div><Label>Name</Label><Input className="mt-1" value={name} onChange={e=>setName(e.target.value)}/></div><div><Label>Email</Label><Input className="mt-1" type="email" value={email} onChange={e=>setEmail(e.target.value)}/></div></CardContent></Card><Card><CardHeader><CardTitle className="flex items-center gap-2 text-base"><Bell className="h-4 w-4 text-primary"/>Application settings</CardTitle></CardHeader><CardContent className="space-y-4"><div><Label>Theme preference</Label><Select value={theme||'dark'} onValueChange={setTheme}><SelectTrigger className="mt-1"><SelectValue/></SelectTrigger><SelectContent><SelectItem value="dark">Dark</SelectItem><SelectItem value="light">Light</SelectItem><SelectItem value="system">System</SelectItem></SelectContent></Select></div><div><Label>Notification preference</Label><Select value={notifications} onValueChange={setNotifications}><SelectTrigger className="mt-1"><SelectValue/></SelectTrigger><SelectContent><SelectItem value="All activity">All activity</SelectItem><SelectItem value="Mentions only">Mentions only</SelectItem><SelectItem value="None">None</SelectItem></SelectContent></Select></div><Button onClick={save}><Save className="mr-2 h-4 w-4"/>Save settings</Button></CardContent></Card></div></main></div></div>}
