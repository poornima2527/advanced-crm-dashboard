import {NextResponse} from 'next/server'; import {getDb} from '@/lib/mongodb'; export async function GET(){const db=await getDb();const c=db.collection('customers');if(await c.countDocuments()===0) return NextResponse.json({total:0,active:0,contactedThisWeek:0,trend:0});const total=await c.countDocuments();const active=await c.countDocuments({status:'Active'});const week=new Date(Date.now()-7*86400000).toISOString().slice(0,10);const contactedThisWeek=await c.countDocuments({lastContactDate:{$gte:week}});return NextResponse.json({total,active,contactedThisWeek,trend:3.2});}
