'use client'; import {useQuery} from '@tanstack/react-query'; import {Card,CardContent} from '@/components/ui/card'; import {Users,UserCheck,PhoneCall} from 'lucide-react'; export function StatsCards(){const {data}=useQuery({queryKey:['stats'],queryFn:()=>fetch('/api/stats').then(r=>r.json())});const cards=[['Total Customers',data?.total??'—',Users,'+3.2%'],['Active Customers',data?.active??'—',UserCheck,'+5.8%'],['Contacted This Week',data?.contactedThisWeek??'—',PhoneCall,'-1.5%']];return <div className="grid gap-4 md:grid-cols-3">{cards.map(([title,value,Icon,trend])=><Card key={title as string}><CardContent className="flex items-center justify-between pt-5"><div><p className="text-sm text-muted-foreground">{title as string}</p><p className="mt-1 text-2xl font-semibold">{value as string|number}</p><p className="mt-2 text-xs text-muted-foreground">Trend <span className={(trend as string).startsWith('-')?'text-rose-400':'text-emerald-400'}>{trend as string}</span></p></div><div className="rounded-lg bg-primary/10 p-2.5 text-primary"><Icon className="h-5 w-5"/></div></CardContent></Card>)}</div>}
