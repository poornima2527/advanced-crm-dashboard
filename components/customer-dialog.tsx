'use client'; import {Dialog,DialogContent,DialogHeader,DialogTitle} from '@/components/ui/dialog'; import {CustomerForm} from './customer-form'; import type {Customer} from '@/types/customer'; export function CustomerDialog({open,onOpenChange,customer,onSave,submitting}:{open:boolean;onOpenChange:(v:boolean)=>void;customer?:Customer;onSave:(d:any)=>void;submitting?:boolean}){return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent><DialogHeader><DialogTitle>{customer?'Edit Customer':'Add Customer'}</DialogTitle></DialogHeader><CustomerForm initial={customer} onSubmit={onSave} submitting={submitting}/></DialogContent></Dialog>}
