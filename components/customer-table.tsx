'use client'; import {useMemo} from 'react'; import {DndContext,closestCenter,PointerSensor,useSensor,useSensors,DragEndEvent} from '@dnd-kit/core'; import {SortableContext,verticalListSortingStrategy,useSortable} from '@dnd-kit/sortable'; import {CSS} from '@dnd-kit/utilities'; import {Table,TableBody,TableCell,TableHead,TableHeader,TableRow} from '@/components/ui/table'; import {Button} from '@/components/ui/button'; import {Badge} from '@/components/ui/badge'; import {StatusBadge} from './status-badge'; import {Pencil,Trash2,GripVertical,ArrowUpDown} from 'lucide-react'; import type {Customer} from '@/types/customer';
export function CustomerTable({customers,onSelect,onEdit,onDelete,onReorder,onSort}:{customers:Customer[];onSelect:(c:Customer)=>void;onEdit:(c:Customer)=>void;onDelete:(c:Customer)=>void;onReorder:(items:Customer[])=>void;onSort?:(key:string)=>void}){const sensors=useSensors(useSensor(PointerSensor,{activationConstraint:{distance:6}}));function dragEnd(e:DragEndEvent){if(!e.over||e.active.id===e.over.id)return;const old=customers.findIndex(x=>x._id===e.active.id),next=customers.findIndex(x=>x._id===e.over!.id);if(old<0||next<0)return;const arr=[...customers];const [m]=arr.splice(old,1);arr.splice(next,0,m);onReorder(arr)}return <DndContext sensors={sensors} collisionDetection={closestCenter} onDragEnd={dragEnd}><SortableContext items={customers.map(c=>c._id!)} strategy={verticalListSortingStrategy}><Table><TableHeader><TableRow><TableHead className="w-8"/><SortHead label="Name" onClick={()=>onSort?.('name')}/><SortHead label="Email" onClick={()=>onSort?.('email')}/><TableHead>Phone</TableHead><TableHead>Company</TableHead><TableHead>Status</TableHead><SortHead label="Last Contact" onClick={()=>onSort?.('lastContactDate')}/><TableHead className="text-right">Actions</TableHead></TableRow></TableHeader><TableBody>{customers.map(c=><SortableRow key={c._id} customer={c} onSelect={onSelect} onEdit={onEdit} onDelete={onDelete}/>)}</TableBody></Table></SortableContext></DndContext>}
function SortableRow({customer:c,onSelect,onEdit,onDelete}:{customer:Customer;onSelect:(c:Customer)=>void;onEdit:(c:Customer)=>void;onDelete:(c:Customer)=>void}){const {attributes,listeners,setNodeRef,transform,transition,isDragging}=useSortable({id:c._id!});return <TableRow ref={setNodeRef} style={{transform:CSS.Transform.toString(transform),transition,opacity:isDragging?.55:1}}><TableCell><button {...attributes} {...listeners} className="cursor-grab p-1 text-muted-foreground"><GripVertical className="h-4 w-4"/></button></TableCell><TableCell><button className="font-medium hover:text-primary" onClick={()=>onSelect(c)}>{c.name}</button></TableCell><TableCell className="max-w-[190px] truncate">{c.email}</TableCell><TableCell>{c.phone}</TableCell><TableCell>{c.company}</TableCell><TableCell><StatusBadge status={c.status}/></TableCell><TableCell>{c.lastContactDate}</TableCell><TableCell><div className="flex justify-end gap-1"><Button variant="ghost" size="icon" onClick={()=>onEdit(c)}><Pencil className="h-4 w-4"/></Button><Button variant="ghost" size="icon" onClick={()=>onDelete(c)}><Trash2 className="h-4 w-4 text-rose-400"/></Button></div></TableCell></TableRow>}

function SortHead({label,onClick}:{label:string;onClick:()=>void}){return <TableHead><button className="inline-flex items-center gap-1 hover:text-foreground" onClick={onClick}>{label}<ArrowUpDown className="h-3.5 w-3.5"/></button></TableHead>}
