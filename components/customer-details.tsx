'use client'; import {Dialog,DialogContent,DialogHeader,DialogTitle} from '@/components/ui/dialog'; import {Button} from '@/components/ui/button'; import {StatusBadge} from './status-badge'; import type {Customer} from '@/types/customer'; import {Mail,Phone,Building2,CalendarDays} from 'lucide-react'; export function CustomerDetails({customer,onClose,onEdit,onDelete}:{customer?:Customer;onClose:()=>void;onEdit:()=>void;onDelete:()=>void}){if(!customer)return null;return <Dialog open={!!customer} onOpenChange={v=>!v&&onClose()}><DialogContent className="max-w-2xl"><DialogHeader><DialogTitle>Customer Details</DialogTitle></DialogHeader><div className="flex items-center gap-4 border-b border-border pb-5"><div className="grid h-14 w-14 place-items-center rounded-full bg-primary text-xl font-bold text-primary-foreground">{customer.name.split(' ').map(x=>x[0]).join('').slice(0,2)}</div><div className="flex-1"><h3 className="text-lg font-semibold">{customer.name}</h3><p className="text-sm text-muted-foreground">{customer.company}</p></div><div className="flex gap-2"><Button variant="destructive" size="sm" onClick={onDelete}>Delete</Button><Button size="sm" onClick={onEdit}>Edit Customer</Button></div></div><div className="grid gap-5 py-5 sm:grid-cols-2"><Info icon={Mail} label="Email" value={customer.email}/><Info icon={Phone} label="Phone" value={customer.phone}/><Info icon={Building2} label="Company" value={customer.company}/><div><p className="text-xs text-muted-foreground">Status</p><div className="mt-1"><StatusBadge status={customer.status}/></div></div><Info icon={CalendarDays} label="Last Contact" value={customer.lastContactDate}/></div><div className="rounded-lg border border-border bg-background p-4"><p className="mb-2 text-sm font-medium">Notes & Interactions</p><p className="text-sm leading-6 text-muted-foreground">{customer.notes||'No notes recorded for this customer.'}</p></div></DialogContent></Dialog>}
function Info({icon:Icon,label,value}:{icon:any;label:string;value:string}){return <div><p className="flex items-center gap-2 text-xs text-muted-foreground"><Icon className="h-3.5 w-3.5"/>{label}</p><p className="mt-1 break-all text-sm">{value}</p></div>}
